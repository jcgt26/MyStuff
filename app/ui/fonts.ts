import {Inter, Lusitana} from "next/font/google";

export const inter = Inter({subsets: ['latin']}) // in case want to import extra char like cyrillic has to happen here
export const lusitana = Lusitana({subsets: ['latin'], weight: ['400']})
